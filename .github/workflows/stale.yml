# Disabled on this fork: no issue triage automation.
# Upstream still uses actions/stale; re-enable only if you want it.
name: Close Stale Issues

on:
  workflow_dispatch:

jobs:
  stale:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Stale workflow disabled on this fork"
